# Copy to keystore.properties (which git ignores) and fill in. Without it, release builds
# come out unsigned. storeFile is relative to the project root.
storeFile=splitsideloader.jks
storePassword=
keyAlias=
keyPassword=
